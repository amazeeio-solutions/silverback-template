<?php

/**
 * @file
 * Install, update and uninstall functions for the test_content module.
 */

/**
 * Implements hook_requirements().
 */
function test_content_requirements($phase) {
  return [
    'module' => [
      'title' => 'custom_default_content module should never be enabled',
      'description' => 'custom_default_content module should never be enabled',
      'severity' => REQUIREMENT_ERROR,
    ],
  ];
}
