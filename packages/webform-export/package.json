{
  "name": "@custom/webform-export",
  "type": "module",
  "private": true,
  "scripts": {
    "test:static": "tsc --noEmit && eslint . --quiet",
    "webform-snapshots": "playwright install chromium && CI=true playwright test",
    "webform-snapshots:headed": "playwright install chromium && playwright test --headed"
  },
  "devDependencies": {
    "@amazeelabs/save-webpage": "^1.2.10",
    "@custom/eslint-config": "workspace:*",
    "@playwright/test": "^1.50.0",
    "@types/node": "^20",
    "typescript": "^5.7.3"
  }
}
