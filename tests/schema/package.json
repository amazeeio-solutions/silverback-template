{
  "name": "@custom-tests/schema",
  "private": true,
  "type": "module",
  "scripts": {
    "test:static": "tsc --noEmit && eslint . --quiet",
    "test:integration": "CI=true vitest",
    "test:integration:update": "CI=truevitest -u",
    "dev": "vitest"
  },
  "devDependencies": {
    "@custom/eslint-config": "workspace:*",
    "@custom/cms": "workspace:*",
    "@types/node": "^20",
    "@vitest/ui": "^3.0.4",
    "got": "^14.4.5",
    "noop-tag": "^2.0.1",
    "start-server-and-test": "^2.0.10",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
